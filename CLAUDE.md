# [Claude Code 작업 지시서] MH Analyzer — OEM MH 옵션 조립비 산출 자동화

- 기획자: 이제명 (HD건설기계, 글로벌생산기술팀 매니저)
- 출처: "생성형 AI 기반 업무 자동화 교육" 과제 제출 메일(2026-08-27), 제목 "RE: [생성형 AI 기반 업무 자동화 교육] 과제 제출 건_이제명"
- 프로젝트 유형: 엑셀 업로드 + 조인/비교 계산 + 엑셀 산출
- 대체 대상: ERP·MES 생산 데이터를 사람이 직접 대조해 OEM MH(작업시간) 계약용 옵션 조립비를 산출하던 일

> ⚠ **이 문서는 기획서 원문을 그대로 옮긴 게 아니다.**
> 원본 코드는 제출자 개인 GitHub 코드스페이스 링크(`codespaces.new/ljm6899a/MH_Analyzer`)로
> 왔는데 비공개(또는 미생성)라 접근할 수 없었다. 그래서 **이메일 본문 텍스트와 첨부
> 스크린샷에 찍힌 화면 구조·컬럼명**만으로 처음부터 다시 만들었다 — 01~07번 프로젝트가
> 기획서 텍스트만으로 제작된 것과 같은 방식이다. 화면에 찍혀 있던 **실제 회사 데이터
> 값**(품번 "2114-1058D68" 등)은 절대 옮기지 않았고, 구조·컬럼명만 가져와 전부 가상
> 데이터(`scripts/make_samples.py`)로 채웠다.

---

## 0. Claude Code에게 — 작업 방식

1. 조용히 빠뜨리지 않는다 — Item Master 조인에서 STM분류가 안 맞으면
   `unmatchedCategories`, BOM MH 합산에서 (품번,개수)가 안 맞으면 `unmatched`로
   화면에 그대로 드러낸다. 0으로 채우지 않는다.
2. 계산 규칙은 `js/logic.js`에 모으고 `test/logic.test.js`로 고정한다.
   화면(`js/app.js`)은 판정하지 않는다.
3. **사내 실데이터를 저장소에 넣지 않는다.** 예제는 `scripts/make_samples.py`가 만든
   가짜 값 6종을 쓴다.
4. 파일은 서버로 올라가지 않는다 — 읽기도 엑셀 만들기도 브라우저 안에서 끝난다.

## 1. 한 줄 요약

품목 원장 + STM분류별 공수(MH) 테이블을 조인해 **Item Master**를 만들고,
Old/New **BOM**을 이 Item Master로 환산해 Assy별 MH 변동을 내고,
기종별 **대표사양** 선정 결과를 비교하는 도구.

## 2. 이메일 원문 요구사항 (5개 탭)

1. **Item Master** — 단품 품번 및 그 개수당 MH 산출 테이블 작성
2. **BOM 비교** — Assy 및 하위 BOM 자재 설변에 따른 MH 변동사항 추출
3. **대표사양** — 1대분 MH 계산을 위한 기준 대표사양 선정
4. **MH 산출** — 전분기 대비 MH 변동사항 가시화 및 최종 MH 산출
5. **MH 옵션 분석** — 옵션별 MH 변동 분석

## 3. 이번 회차에서 구현한 것 / 다음 단계로 넘긴 것

- **① Item Master, ② BOM 비교, ③ 대표사양** — 계산 로직·화면·테스트 전부 구현 완료.
  `js/logic.js`의 `buildItemMaster`·`compareAssyMH`·`diffRepSpec`가 핵심이고
  `test/logic.test.js` 8개로 고정했다(수기 계산과 대조해 검증 완료 — Dev_md 참고).
- **④ MH 산출, ⑤ MH 옵션 분석** — 화면에 "다음 단계" 안내만 두고 **구현하지 않았다**.
  ①~③ 결과를 조합하는 로직인데, 이메일 원문만으로는 "1대분 최종 MH"를 어떻게
  집계하는지(대표사양 조합과 BOM MH를 어떤 공식으로 곱/합산하는지) 확정할 근거가
  부족했다. 다섯 탭을 전부 어설프게 만들기보다 앞 세 탭을 정확히 만드는 쪽을
  택했다 — 다음 회차에 제출자에게 ④·⑤의 정확한 계산 공식을 확인한 뒤 이어서
  구현할 것.

## 4. 데이터 스키마

- **품목 원장**: 품번, 품명, STM분류
- **공수(MH) 테이블**: STM분류, 개수, 작업시간(MH)
- **BOM**: Assy, 품번, 품명, 개수
- **대표사양 언피벗**: 기종, 옵션코드, 옵션값, 옵션명, 값이름, 대표사양(TRUE/FALSE)

## 부록 A — 기획서 원문 (이메일 발췌)

> 개요: ERP 및 MES의 생산 데이터를 활용하여 OEM MH 계약시 사용하는 옵션 조립비 산출 자동화
>
> Item Master: 단품 품번 및 그 개수당 MH 산출 테이블 작성
> BOM 비교: Assy 및 Assy 하위 BOM 자재 설변에 따른 MH 변동사항 추출
> 대표사양: 1대분 MH 계산을 위한 기준 대표사양 선정
> MH 산출: 전분기 대비 MH 변동사항 가시화 및 최종 MH 산출
> MH 옵션 분석: 옵션별 MH 변동 분석
